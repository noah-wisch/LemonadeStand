module.exports = {
    name: 'ManagerService',
    func($http, NewGameService) {
        const gameStats = [];
        let identifier = null;

    },
    currentId(id) {
        identifier = id;
    },
};